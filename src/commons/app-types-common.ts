export type T_DATABASE_CREDENTIALS = {
  DB_HOST: string | undefined,
  DB_NAME: string | undefined,
  DB_USERNAME: string | undefined,
  DB_PASSWORD: string | undefined,
  DB_PORT: number | undefined,
}
